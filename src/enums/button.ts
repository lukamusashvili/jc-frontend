export enum Auth {
    LOGIN = "შესვლა",
    LOGOUT = "გასვლა",
}

export enum Nav {
    PRODUCTS = "პროდუქტები",
    FINANCES = "ფინანსები",
    ANALYTICS = "ანალიტიკა",
    BALANCE = "ბალანსი",
}

export enum Product {
    PRODUCT = "პროდუქტი",
    PRODUCT_ADD = "პროდუქტის დამატება",
}

export enum Transaction {
    TRANSACTION = "ტრანზაქცია",
    TRANSACTION_ADD = "ტრანზაქციის დამატება",
}

export enum Actions {
    GO_BACK = "უკან",
    YES = "დიახ",
    ADD = "დამატება",
    CANCEL = "გაუქმება",
}
